"use client";
/* eslint-disable react-hooks/purity -- deadline comparisons intentionally use the current client time */

import {FormEvent,useCallback,useEffect,useMemo,useState} from "react";
type Item={id:number;requestNumber:string;name:string;email:string;phone:string;requestType:string;details:string;privacyAcknowledgedAt:string;orderNumber:string;orderId:number|null;identityStatus:string;status:string;dueAt:string;assignedTo:string;adminNote:string;responseSummary:string;resolvedAt:string|null;createdAt:string;updatedAt:string};
const typeLabels:Record<string,string>={access:"Erişim",correction:"Düzeltme",deletion:"Silme",processing_objection:"İtiraz",other:"Diğer"};const statusLabels:Record<string,string>={new:"Yeni",reviewing:"İnceleniyor",waiting_identity:"Kimlik doğrulama bekliyor",completed:"Tamamlandı",rejected:"Gerekçeli ret"};
function RequestCard({item,onSaved}:{item:Item;onSaved:()=>Promise<void>}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const overdue=!item.resolvedAt&&new Date(item.dueAt).getTime()<Date.now();async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/privacy-requests",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id:item.id,...values})});const body=await response.json();setMessage(response.ok?"Veri talebi güncellendi.":body.error??"Talep güncellenemedi.");if(response.ok)await onSaved();setBusy(false)}return <article className={`admin-card privacy-request-card ${overdue?"overdue":""}`}><header><div><span>{item.requestNumber} · {typeLabels[item.requestType]??item.requestType}</span><h2>{item.name}</h2><p>{item.email}{item.phone?` · ${item.phone}`:""}</p><small>{item.privacyAcknowledgedAt?`Gizlilik onayı: ${new Date(item.privacyAcknowledgedAt).toLocaleString("tr-TR")}`:"Eski kayıt · onay zamanı yok"}</small></div><div><strong>{overdue?"Süre aşıldı":statusLabels[item.status]??item.status}</strong><time>Son tarih: {new Date(item.dueAt).toLocaleDateString("tr-TR")}</time></div></header><blockquote>{item.details}</blockquote><div className="privacy-request-links">{item.orderId?<a href={`/admin/siparis/${item.orderId}`}>{item.orderNumber} siparişini aç →</a>:item.orderNumber?<span>{item.orderNumber} e-posta ile eşleşmedi</span>:<span>Sipariş bağlantısı yok</span>}<a href={`mailto:${item.email}?subject=${encodeURIComponent(`${item.requestNumber} veri talebi`)}`}>E-posta ile yanıtla ↗</a></div><form onSubmit={save}><label>Durum<select name="status" defaultValue={item.status}><option value="new">Yeni</option><option value="reviewing">İnceleniyor</option><option value="waiting_identity">Kimlik doğrulama bekliyor</option><option value="completed">Tamamlandı</option><option value="rejected">Gerekçeli ret</option></select></label><label>Kimlik kontrolü<select name="identityStatus" defaultValue={item.identityStatus}><option value="unverified">Doğrulanmadı</option><option value="pending">Kontrol ediliyor</option><option value="verified">Doğrulandı</option><option value="rejected">Doğrulanamadı</option></select></label><label>Sorumlu<input name="assignedTo" defaultValue={item.assignedTo}/></label><label className="wide">İç not<textarea name="adminNote" rows={3} defaultValue={item.adminNote} placeholder="Kimlik belgesi veya hassas veri eklemeyin."/></label><label className="wide">Cevap özeti<textarea name="responseSummary" rows={3} defaultValue={item.responseSummary} placeholder="Tamamlama veya ret halinde zorunludur."/></label><button disabled={busy}>{busy?"Kaydediliyor…":"Talebi güncelle"}</button></form>{message&&<p role="status">{message}</p>}</article>}
export default function PrivacyRequestCenter(){const[items,setItems]=useState<Item[]>([]);const[filter,setFilter]=useState("open");const[message,setMessage]=useState("Veri talepleri yükleniyor…");const load=useCallback(async()=>{const response=await fetch("/api/privacy-requests");const body=await response.json();if(response.ok){setItems(body.requests);setMessage("")}else setMessage(body.error??"Talepler alınamadı.")},[]);useEffect(()=>{void load()},[load]);const open=items.filter(item=>!["completed","rejected"].includes(item.status));const overdue=open.filter(item=>new Date(item.dueAt).getTime()<Date.now());const visible=useMemo(()=>filter==="all"?items:filter==="overdue"?items.filter(item=>!["completed","rejected"].includes(item.status)&&new Date(item.dueAt).getTime()<Date.now()):filter==="completed"?items.filter(item=>["completed","rejected"].includes(item.status)):items.filter(item=>!["completed","rejected"].includes(item.status)),[filter,items]);return <main className="admin-shell privacy-admin-shell"><header className="admin-header"><div><p>KİŞİSEL VERİ HAKLARI</p><h1>Veri talebi merkezi</h1></div><div><a href="/veri-talebi">Müşteri formunu gör ↗</a><a href="/admin/veri-guvenligi">Veri güvenliği ↗</a><a href="/admin">Panele dön ↗</a></div></header><section className="privacy-admin-notice"><b>Kontrollü değerlendirme</b><p>Silme veya dışa aktarma otomatik yapılmaz. Önce kimlik doğrulanır, üçüncü kişilere ait bilgiler ayrıştırılır ve yasal saklama zorunlulukları değerlendirilir.</p></section><section className="privacy-admin-summary"><article><b>{open.length}</b><span>Açık</span></article><article className={overdue.length?"warning":""}><b>{overdue.length}</b><span>Süresi aşılmış</span></article><article><b>{items.filter(item=>item.identityStatus==="verified").length}</b><span>Kimliği doğrulanan</span></article><article><b>{items.filter(item=>["completed","rejected"].includes(item.status)).length}</b><span>Sonuçlanan</span></article></section><nav className="admin-card privacy-admin-filters">{[["open","Açık"],["overdue","Süresi aşılmış"],["completed","Sonuçlanan"],["all","Tümü"]].map(([value,label])=><button className={filter===value?"active":""} onClick={()=>setFilter(value)} key={value}>{label}</button>)}</nav>{message&&<p className="admin-message">{message}</p>}<section>{visible.map(item=><RequestCard item={item} onSaved={load} key={`${item.id}-${item.updatedAt}`}/>)}{visible.length===0&&<p className="admin-card empty">Bu filtrede veri talebi yok.</p>}</section></main>}
