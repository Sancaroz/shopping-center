"use client";

import { FormEvent, useState } from "react";
type Report={created:number;updated:number;skipped:number;errors:string[]};

export default function Importer(){const[busy,setBusy]=useState(false);const[report,setReport]=useState<Report|null>(null);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setReport(null);const form=event.currentTarget;const response=await fetch("/api/import/products",{method:"POST",body:new FormData(form)});const data=await response.json();if(response.ok){setReport(data);form.reset();}else setReport({created:0,updated:0,skipped:1,errors:[data.error??"Dosya içe aktarılamadı."]});setBusy(false);}return <main className="admin-shell importer-shell"><header className="admin-header"><div><p>TOPLU ÜRÜN YÖNETİMİ</p><h1>CSV ile ürün aktarımı</h1></div><div><a href="/admin">Panele dön ↗</a></div></header><section className="importer-grid"><article className="admin-card importer-guide"><p className="importer-kicker">NASIL ÇALIŞIR?</p><ol><li><span>01</span><div><b>Şablonu indirin</b><p>Sütun adlarını değiştirmeden örnek satırı kendi ürünlerinizle değiştirin.</p></div></li><li><span>02</span><div><b>Excel’de doldurun</b><p>Fiyat, stok, pazar ve kategori kodlarını girin. Dosyayı CSV olarak kaydedin.</p></div></li><li><span>03</span><div><b>Dosyayı yükleyin</b><p>Aynı ürün kodu varsa mevcut kayıt güncellenir; yeni kodlar yeni ürün oluşturur.</p></div></li></ol><a className="template-download" href="/api/import/products">CSV şablonunu indir ↓</a></article><article className="admin-card importer-upload"><p className="importer-kicker">DOSYA YÜKLE</p><h2>Ürün listenizi seçin</h2><p>Tek seferde en fazla 250 ürün ve 2 MB dosya yükleyebilirsiniz. Önce küçük bir listeyle denemenizi öneririz.</p><form onSubmit={submit}><label>CSV dosyası<input name="file" type="file" accept=".csv,text/csv" required/></label><button disabled={busy}>{busy?"Ürünler aktarılıyor…":"İçe aktarmayı başlat"}</button></form>{report&&<div className={report.errors.length?"import-report has-errors":"import-report"}><h3>{report.created} yeni ürün · {report.updated} güncellendi</h3><p>{report.skipped} satır atlandı.</p>{report.errors.length>0&&<ul>{report.errors.map((error,index)=><li key={index}>{error}</li>)}</ul>}<a href="/admin">Ürünleri yönet →</a></div>}</article></section><section className="importer-notes"><h2>CSV alanları</h2><div><p><b>nameTr, slug</b><span>Zorunlu alanlar</span></p><p><b>categorySlug</b><span>Paneldeki kategori koduyla aynı olmalı</span></p><p><b>marketTr, marketGlobal, active</b><span>Evet / Hayır olarak yazılabilir</span></p><p><b>imageUrl</b><span>Görsel bağlantısı; görselleri daha sonra panelden de yükleyebilirsiniz</span></p></div></section></main>;}
