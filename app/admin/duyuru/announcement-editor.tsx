"use client";import{FormEvent,useEffect,useState}from"react";type Values={showAnnouncement:string;announcementTr:string;announcementUrlTr:string;announcementGlobal:string;announcementUrlGlobal:string};const defaults:Values={showAnnouncement:"true",announcementTr:"1.500 TL üzeri ücretsiz gönderim",announcementUrlTr:"/magaza",announcementGlobal:"Complimentary shipping over €150",announcementUrlGlobal:"/magaza"};export default function AnnouncementEditor(){const[values,setValues]=useState(defaults);const[message,setMessage]=useState("Yükleniyor…");const[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/settings").then(r=>r.json()).then(data=>{setValues({...defaults,...data.settings});setMessage("")}).catch(()=>setMessage("Duyuru ayarları yüklenemedi."))},[]);async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const response=await fetch("/api/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(values)});setMessage(response.ok?"Duyuru bandı güncellendi.":"Duyuru ayarları kaydedilemedi.");setBusy(false)}return <main className="admin-shell editor-shell"><header className="admin-header"><div><p>KAMPANYA</p><h1>Duyuru bandı</h1></div><div><a href="/">Ana sayfayı gör ↗</a><a href="/admin">Panele dön ↗</a></div></header><section className="admin-card editor-card"><p className="settings-note">Ücretsiz kargo, yeni koleksiyon veya kısa süreli kampanyalarınızı Türkiye ve Global mağazada ayrı ayrı duyurun.</p><form className="admin-form" onSubmit={save}><label className="wide homepage-section-toggles"><input type="checkbox" checked={values.showAnnouncement==="true"} onChange={e=>setValues({...values,showAnnouncement:String(e.target.checked)})}/> Duyuru bandını göster</label><fieldset className="wide admin-form"><legend>Türkiye</legend><label className="wide">Duyuru metni<input value={values.announcementTr} onChange={e=>setValues({...values,announcementTr:e.target.value})} required/></label><label className="wide">Hedef bağlantı<input value={values.announcementUrlTr} onChange={e=>setValues({...values,announcementUrlTr:e.target.value})} placeholder="/magaza" required/></label></fieldset><fieldset className="wide admin-form"><legend>Global</legend><label className="wide">Announcement text<input value={values.announcementGlobal} onChange={e=>setValues({...values,announcementGlobal:e.target.value})} required/></label><label className="wide">Target link<input value={values.announcementUrlGlobal} onChange={e=>setValues({...values,announcementUrlGlobal:e.target.value})} placeholder="/magaza" required/></label></fieldset><button disabled={busy}>{busy?"Kaydediliyor…":"Duyuru bandını kaydet"}</button></form>{message&&<p className="admin-message">{message}</p>}</section></main>}
