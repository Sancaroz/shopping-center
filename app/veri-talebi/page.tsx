"use client";

import {FormEvent,useRef,useState} from "react";
import "./veri-talebi.css";

export default function PrivacyRequestPage(){const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[result,setResult]=useState<{requestNumber:string;dueAt:string}|null>(null);const requestKey=useRef("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");if(!requestKey.current)requestKey.current=crypto.randomUUID();const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/privacy-requests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({...values,requestKey:requestKey.current,privacyAcknowledged:values.privacyAcknowledged==="on"})});const body=await response.json();if(response.ok){requestKey.current="";setResult(body)}else setError(body.error??"Talep kaydedilemedi.");setBusy(false)}return <main className="privacy-page"><header><a href="/">MYSA <span>OBJETS</span></a><nav><a href="/politikalar#gizlilik">Gizlilik</a><a href="/iletisim">İletişim</a></nav></header><section className="privacy-hero"><p>KİŞİSEL VERİLER</p><h1>Verilerinizle ilgili<br/><em>talebinizi iletin.</em></h1><span>Erişim, düzeltme, silme veya işlemeye itiraz taleplerinizi güvenli bir operasyon kaydı olarak mağaza yönetimine iletin.</span></section><section className="privacy-shell">{result?<article className="privacy-success"><p>TALEBİNİZ ALINDI</p><h2>{result.requestNumber}</h2><span>Başvurunuz kaydedildi. Kimlik doğrulaması gerekirse verdiğiniz e-posta adresi üzerinden sizinle iletişime geçilir. Bu numarayı saklayın.</span><a href="/">Mağazaya dön →</a></article>:<><aside><h2>Bilmeniz gerekenler</h2><p>Kart bilgisi, parola veya kimlik belgesi yüklemeyin. Silme talepleri, mevzuat gereği saklanması zorunlu kayıtları otomatik olarak silmez.</p><p>Başvurular talebin niteliğine göre en kısa sürede ve en geç 30 gün içinde değerlendirilir.</p></aside><form onSubmit={submit}><label>Ad soyad<input name="name" autoComplete="name" maxLength={120} required/></label><label>E-posta<input name="email" type="email" autoComplete="email" maxLength={180} required/></label><label>Telefon <small>İsteğe bağlı</small><input name="phone" autoComplete="tel" maxLength={40}/></label><label>Sipariş numarası <small>Varsa</small><input name="orderNumber" maxLength={18} pattern="MS-[0-9]{8}-[A-Z0-9]{6}" placeholder="MS-20260726-ABC123"/></label><label className="wide">Talep türü<select name="requestType" defaultValue="" required><option value="" disabled>Seçin</option><option value="access">Verilerime erişim</option><option value="correction">Verilerimin düzeltilmesi</option><option value="deletion">Verilerimin silinmesi</option><option value="processing_objection">Veri işlemeye itiraz</option><option value="other">Diğer</option></select></label><label className="wide">Talebiniz<textarea name="details" rows={7} minLength={10} maxLength={4000} required/></label><label className="privacy-trap" aria-hidden="true">Company<input name="company" tabIndex={-1} autoComplete="off"/></label><label className="privacy-consent wide"><input name="privacyAcknowledged" type="checkbox" required/><span>Bu formdaki bilgilerin talebimi değerlendirmek ve benimle iletişim kurmak amacıyla kaydedileceğini anladım.</span></label>{error&&<p className="privacy-error wide" role="alert">{error}</p>}<button className="wide" disabled={busy}>{busy?"Kaydediliyor…":"Veri talebini gönder"}</button></form></>}</section></main>}
